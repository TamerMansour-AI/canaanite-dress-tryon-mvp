{
  "name": "canaanite-dress-tryon-mvp",
  "private": true,
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix server run start\" \"npm --prefix app run dev\"",
    "dev:app": "npm --prefix app run dev",
    "build:app": "npm --prefix app run build",
    "start:app": "npm --prefix app run start",
    "start:server": "npm --prefix server run start",
    "install:all": "npm install --workspaces"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
